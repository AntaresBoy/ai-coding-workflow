# Codex Bundle: openspec-feature

Install this bundle when you want the `openspec-feature-workflow` Codex skill.

Install target:

```text
.codex/skills/
```

After installation, invoke:

```text
Use $openspec-feature-workflow to run this feature.
```
