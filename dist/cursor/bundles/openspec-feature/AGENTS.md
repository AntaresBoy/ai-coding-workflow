# Agent Workflow

This workflow is explicit opt-in. Do not apply it by default. Only apply it when the user explicitly asks for this workflow or names it in chat.

When the user asks for the `openspec-feature-workflow` workflow:

See `.cursor/rules/openspec-feature-workflow.mdc` for the full workflow definition.
